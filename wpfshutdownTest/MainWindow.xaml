<Window x:Class="wpfshutdownTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wpfshutdownTest"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.Resources>
            <Style x:Key="button" TargetType="Button">
                <Setter Property="FontSize" Value="18"></Setter>
            </Style>
        </Grid.Resources>
        <Button Grid.Row="0" Click="ShutDown" Grid.Column="0" Style="{StaticResource button}" Content="ShutDown··"></Button>
        <Button Grid.Row="0" Click="BtnMoTai" Grid.Column="1" Style="{StaticResource button}" Content="模态窗体··"></Button>
        <Button Grid.Row="0" Click="BtnNoMoTai" Grid.Column="2" Style="{StaticResource button}" Content="非模态窗体··"></Button>
    </Grid>
</Window>
