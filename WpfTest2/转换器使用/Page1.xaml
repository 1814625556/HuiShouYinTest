<Page x:Class="WpfTest2.转换器使用.Page1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:entity="clr-namespace:WpfTest2.转换器使用.Entitys"
      d:DataContext="{d:DesignInstance entity:PersonCC}"
      xmlns:converts="clr-namespace:WpfTest2.转换器使用.Converts"
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="400"
      Title="Page1">
    <Page.Resources>
        <converts:StringConvert1 x:Key="StringConvert1"></converts:StringConvert1>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Page.Resources>
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Button Content="{Binding Name ,Converter={StaticResource StringConvert1}}" Visibility="{Binding IsVisual ,Converter={StaticResource BooleanToVisibilityConverter}}"></Button>
        <Button Grid.Row="1" Margin="10" Content="ceshi" Click="ceshi"></Button>
    </Grid>
</Page>

